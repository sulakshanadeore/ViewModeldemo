@model IEnumerable<ViewModeldemo.Models.CustModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Custid)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Custname)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.City)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RegistrationDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Custid)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Custname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.City)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RegistrationDate)
        </td>
        <td>
            
            @Html.ActionLink("Editcustomer", "AddOrderDetails", new {  id=item.Custid }) |
            @Html.ActionLink("ShowDetails", "Details", new { id = item.Custid }) |
            @Html.ActionLink("Delete", "Delete", new {  id=item.Custid })
        </td>
    </tr>
}

</table>
